# Build locally, then configure the immutable sha256 image ID. Runtime execution
# never pulls an image. Debian/Ubuntu FFmpeg package licensing is retained.
FROM ubuntu@sha256:008173c23f95b170204355c12626cb5a965d779a7e1283b09e9cffbb1bf33ca3
RUN apt-get update && DEBIAN_FRONTEND=noninteractive apt-get upgrade -y && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends ffmpeg ca-certificates && rm -rf /var/lib/apt/lists/*
USER 65532:65532
WORKDIR /work
ENTRYPOINT ["/bin/sleep"]
CMD ["300"]
