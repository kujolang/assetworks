# Next-work implementation — 2026-09-23

This session follows the seven-item [round-two worklist](REVIEW_FOLLOWUP_2026-09-22_ROUND_2.md). Changes are on development `main`; the existing v0.3.0 release remains unchanged.

| Item | Outcome |
| --- | --- |
| npm publication | Public resolver and darwin-arm64 packages still report 1.4.0. Prior publisher run 35793388734 remains failed. Trusted-publisher/permission review is required; no credential substitution or registry publication is claimed. |
| Resumable maintenance | Explicit `--resume`, immutable job identity, byte-for-byte reuse checks, source-deletion/conflict/link refusal, bounded progress receipts and atomic evidence-preserving quarantine. |
| Codec update automation | Weekly/manual rebuild and actual isolation/recovery rehearsal; Kujo-generated installed-package SPDX evidence; [snapshot evaluation and promotion policy](CODEC_UPDATES.md). |
| Trust freshness evaluation | [Decision and acceptance criteria](TRUST_DISTRIBUTION_REVIEW.md): retain explicit operator policy; signed distribution needs independent root trust, rollback-resistant version storage and a clock policy. No unsupported freshness or timestamp claims. |
| Large-state costs | New sibling-process benchmark measures valid-record audits, backup and restore for uniform versus deliberately single-shard state; receipts and operating guidance are recorded below. |
| Media/accessibility | Native offline SRT ↔ plain WebVTT conversion and transcript extraction with CC0 fixtures, strict timing/format limits, deterministic bytes and provenance. Existing codec coverage retained. |
| Recovery exercises | `drill` restores into a new directory, retains inspection evidence and emits a path-free monitoring summary. Weekly fixture drills cover missing backup/trust and interruption before publication. Real-data scheduling/retention remains operator-owned. |

## Verification

Verification receipts will be finalized after the complete local and hosted gates finish. Existing release verification remains in [the release acceptance record](NEXT_SESSION_IMPLEMENTATION.md).

## Remaining work

1. Resolve the existing npm publishing authorization incident, then run the npm installed-package matrix. This is the only carried distribution blocker.
2. Exercise the new maintenance and recovery commands against an operator-selected production dataset and storage topology; establish deployment-specific maintenance windows and retention.
3. If automated trust distribution is required, supply the policy decisions identified in the trust review before implementing an authenticated updater or timestamp authority integration.
4. Promote tested codec images through the documented review process; add an approved vulnerability scanner/license review if the deployment requires those attestations.

These operational choices are not universal enterprise certification, and successful synthetic drills do not certify production backups.
