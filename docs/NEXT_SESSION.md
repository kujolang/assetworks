# AssetWorks next-session worklist

- [ ] Define an adapter conformance suite for FFmpeg and image-processing providers.
- [ ] Add streaming checksum support after benchmarking files above the current 64 MiB bound.
- [ ] Add deterministic media-probe fixtures for duration, dimensions, and codecs.
- [ ] Add signed manifests without making signing keys a baseline dependency.
- [ ] Run multi-process contention benchmarks on Linux, macOS, and Windows.
